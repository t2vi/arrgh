{
  "name": "arrgh-plugin-toonily",
  "version": "1.0.0",
  "description": "Toonily manhwa source plugin for *ARRgh (plugin-host bundle format)",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --target=node22 --outfile=bundles/toonily.js"
  },
  "dependencies": {
    "cheerio": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "esbuild": "^0.21.0",
    "typescript": "^5.5.0"
  }
}
