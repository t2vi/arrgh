{
  "name": "arrgh-plugin-comick",
  "version": "1.0.0",
  "description": "Comick source plugin for *ARRgh (plugin-host bundle format)",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --target=node22 --outfile=bundles/comick.js"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "esbuild": "^0.21.0",
    "typescript": "^5.5.0"
  }
}
