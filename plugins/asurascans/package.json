{
  "name": "arrgh-plugin-asurascans",
  "version": "1.0.0",
  "description": "AsuraScans source plugin for *ARRgh — manhwa",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --target=node22 --outfile=bundles/asurascans.js"
  },
  "dependencies": {
    "cheerio": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "esbuild": "^0.21.0",
    "typescript": "^5.5.0"
  }
}
