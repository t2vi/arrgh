{
  "name": "arrgh-live-tests",
  "private": true,
  "version": "1.0.0",
  "description": "Live source snapshot tests — real HTTP calls, not run in CI",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:update": "vitest run --update-snapshots",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "playwright-core": "^1.44.0",
    "tsx": "^4.15.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
