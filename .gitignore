# ── Rust / Server ────────────────────────────────────────────
server/target/
server/.env
server/arrgh.db
server/downloads/
server/static/
server/arrgh.db

# ── .NET Server ───────────────────────────────────────────────
server-dotnet/bin/
server-dotnet/obj/
server-dotnet/*.db
server-dotnet/*.db-shm
server-dotnet/*.db-wal
server-dotnet/.env
server-dotnet-tests/bin/
server-dotnet-tests/obj/
server-dotnet-tests/*.db

# ── Web (React + Vite) ───────────────────────────────────────
web/node_modules/
web/dist/
web/allure-results/
web/coverage/

# ── Workspace ────────────────────────────────────────────────
node_modules/

# ── Plugins ──────────────────────────────────────────────────
plugins/*/node_modules/
plugins/*/bundles/
plugins/*/dist/
plugins/*/.env
plugin-host/bundles/
web/.env
web/.env.local
web/tsconfig.*.tsbuildinfo

# ── Viewer (Flutter) ─────────────────────────────────────────
app/.dart_tool/
app/.flutter-plugins
app/.flutter-plugins-dependencies
app/build/
app/.packages
app/pubspec.lock

# ── Claude Code ──────────────────────────────────────────────
.agents/
.claude/
docs/adr
docs/agents
CLAUDE.md
CONTEXT.md
graphify-out/
skills-lock.json
.understand-anything

# ── General ──────────────────────────────────────────────────
.DS_Store
*.log
*.db-shm
*.db-wal
*.cbz
habi.config.json