# ── Server (.NET) ─────────────────────────────────────────────
server/bin/
server/obj/
server/*.db
server/*.db-shm
server/*.db-wal
server/.env
server/downloads/
server-tests/bin/
server-tests/obj/
server-tests/*.db

# ── Web (React + Vite) ───────────────────────────────────────
web/node_modules/
web/dist/
web/allure-results/
web/coverage/

# ── Workspace ────────────────────────────────────────────────
node_modules/

# ── Plugins ──────────────────────────────────────────────────
plugins/*/node_modules/
plugins/*/bundles/
plugins/*/dist/
plugins/*/.env
plugin-host/bundles/
web/.env
web/.env.local
web/tsconfig.*.tsbuildinfo

# ── Viewer (Flutter) ─────────────────────────────────────────
app/.dart_tool/
app/.flutter-plugins
app/.flutter-plugins-dependencies
app/build/
app/.packages
app/pubspec.lock

# ── Claude Code / dev-private ────────────────────────────────
# AI/dev files kept out of this public repo. Real files on disk, mirrored to
# the private repo t2vi/arrgh-dev via scripts/private-overlay.sh. The
# managed block below is regenerated from .claude/private-overlay.conf.
CONTEXT.MD
.claude/settings.local.json

# ── General ──────────────────────────────────────────────────
.DS_Store
*.log
*.db-shm
*.db-wal
*.cbz
habi.config.json



# >>> private-overlay >>>
# AI/dev files tracked in the private overlay (scripts/private-overlay.sh) — never public.
# Regenerated from .claude/private-overlay.conf; edit that, not this block.
.private.git/
.private-tmp/
CLAUDE.md
CONTEXT.md
skills-lock.json
.agents
.understand-anything
graphify-out
.claude
docs/adr
docs/agents
api-live-tests
live-tests
# <<< private-overlay <<<
